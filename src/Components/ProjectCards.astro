---

export interface Props {
    title: string;
    description: string;
    link: string;
}
const { title, description, link } = Astro.props;
---
<div class="bg-[#222323] text-white p-6 rounded-lg shadow-lg hover:bg-orange-500 hover:shadow-2xl transition duration-300 transform hover:scale-105">
    <h3 class="font-semibold text-xl mb-4">{title}</h3>
    <p class="text-gray-200 mb-4">{description}</p>
    <a href={link} class="text-light-blue hover:text-dark-blue underline">Ver más →</a>
</div>

